import { MonthlyReportsManager } from "@/components/monthly-reports/monthly-reports-manager";

export const metadata = {
  title: "Monthly Reports | Hadona Workspace",
};

export default function MonthlyReportsPage() {
  return <MonthlyReportsManager />;
}